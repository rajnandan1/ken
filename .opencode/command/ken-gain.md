---
description: Show ken's measured impact scoreboard from its own benchmark results
---

Show the ken gain scoreboard without changing mode or writing files. Render medians from ken's runs in benchmarks/results/ as plain ASCII bars (bar length = measured range, label = recorded figure). If that directory has no ken results, print 'ken gain: no measured numbers yet. Nothing is claimed that was not measured.' and point to the harness in benchmarks/ (needs API keys in .env). Do not print per-repo savings because the unwritten version provides no baseline. Point readers to /ken-debt for the counted ceilings ledger and /ken-audit for current rewrite candidates. Do not present another project's numbers as ken's. Report only.
